#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <assert.h>
#include "CNN.h"
#include "load_params.h"
#include "Math.h"

int main(int argc, char **argv) {
    /* init_params(); */
    /* init_weights(); */
    /* print_weights(); */
    /* struct ParamInfo p = params[CONV10W_IDX]; */
    /* int idx = get_idx(p.dimensions, p.dim_len, (int []){13, 0, 1, 2, 0}); */
    /* float val = p.weights[idx]; */
    /* printf("%f", val); */
    /* printf(params[CONV10W_IDX]) */

    /* get_output_shape((int[]){128,1,200,5,5}, 16, 3,1,1,1); */
    return EXIT_SUCCESS;
}
